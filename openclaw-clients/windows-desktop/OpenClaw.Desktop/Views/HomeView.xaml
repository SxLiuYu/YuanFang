<UserControl x:Class="OpenClaw.Desktop.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <ScrollViewer Margin="16">
        <StackPanel>
            <!-- 欢迎区域 -->
            <Border Style="{StaticResource MaterialDesignElevationDepth1}" Padding="24" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock FontSize="28" FontWeight="Bold">
                            <Run Text="{Binding Greeting}"/>
                            <Run Text="，"/>
                            <Run Text="{Binding UserName}"/>
                        </TextBlock>
                        <TextBlock Text="{Binding CurrentDate}" 
                                  Style="{DynamicResource MaterialDesignCaptionTextBlock}"
                                  Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <TextBlock Grid.Column="1" Text="🐾" FontSize="48" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- 快捷操作 -->
            <TextBlock Text="快捷操作" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>
            <UniformGrid Columns="4" Margin="0,0,0,24">
                <Button Command="{Binding NavigateToVoiceCommand}"
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       Height="80" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="🎤" FontSize="24" HorizontalAlignment="Center"/>
                        <TextBlock Text="语音控制" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding NavigateToHealthCommand}"
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       Height="80" Margin="8,0,8,0">
                    <StackPanel>
                        <TextBlock Text="❤️" FontSize="24" HorizontalAlignment="Center"/>
                        <TextBlock Text="健康管理" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding NavigateToFinanceCommand}"
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       Height="80" Margin="8,0,8,0">
                    <StackPanel>
                        <TextBlock Text="💰" FontSize="24" HorizontalAlignment="Center"/>
                        <TextBlock Text="财务管理" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding NavigateToTasksCommand}"
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       Height="80" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="✅" FontSize="24" HorizontalAlignment="Center"/>
                        <TextBlock Text="任务管理" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Button>
            </UniformGrid>

            <!-- 今日概览 -->
            <TextBlock Text="今日概览" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>
            <UniformGrid Columns="3" Margin="0,0,0,24">
                <Border Style="{StaticResource MaterialDesignElevationDepth1}" Padding="16" Margin="0,0,8,0" Background="#E3F2FD">
                    <StackPanel>
                        <TextBlock Text="今日步数" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>
                        <TextBlock Text="{Binding TodaySteps, StringFormat={}{0:N0}}" 
                                  FontSize="28" FontWeight="Bold" Foreground="#1976D2"/>
                        <TextBlock Text="步" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource MaterialDesignElevationDepth1}" Padding="16" Margin="8,0,8,0" Background="#E8F5E9">
                    <StackPanel>
                        <TextBlock Text="今日支出" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>
                        <TextBlock Text="{Binding TodayExpense, StringFormat=¥{0:N2}}" 
                                  FontSize="28" FontWeight="Bold" Foreground="#388E3C"/>
                        <TextBlock Text="元" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource MaterialDesignElevationDepth1}" Padding="16" Margin="8,0,0,0" Background="#FFF3E0">
                    <StackPanel>
                        <TextBlock Text="待办任务" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>
                        <TextBlock Text="{Binding PendingTasks}" 
                                  FontSize="28" FontWeight="Bold" Foreground="#F57C00"/>
                        <TextBlock Text="项" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- 统计卡片 -->
            <TextBlock Text="本周统计" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>
            <Border Style="{StaticResource MaterialDesignElevationDepth1}" Padding="16" Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,16,16">
                        <TextBlock Text="已完成任务" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>
                        <TextBlock Text="{Binding CompletedTasks}" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="16,0,0,16">
                        <TextBlock Text="待处理任务" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>
                        <TextBlock Text="{Binding PendingTasks}" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,16,16,0">
                        <TextBlock Text="完成率" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>
                        <TextBlock Text="70%" FontSize="24" FontWeight="Bold" Foreground="#4CAF50"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="16,16,0,0">
                        <TextBlock Text="本周支出" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>
                        <TextBlock Text="¥3,250" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 最近活动 -->
            <TextBlock Text="最近活动" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>
            <Border Style="{StaticResource MaterialDesignElevationDepth1}" Padding="0">
                <ItemsControl ItemsSource="{Binding RecentActivities}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Padding="16,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" Text="•" FontSize="16" 
                                              Foreground="{DynamicResource PrimaryHueMidBrush}"
                                              VerticalAlignment="Top" Margin="0,0,12,0"/>
                                    
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding TimeDisplay}" 
                                                  Style="{DynamicResource MaterialDesignCaptionTextBlock}"
                                                  Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                  Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>